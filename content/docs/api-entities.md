# Entities API

Read profiled entities and risk history from your organization.

**Base path:** `/api/public/v1/entities`

**Authentication:** `X-API-Key` with **read** scope.

## Endpoints

The reference below lists each endpoint with parameters and example `curl` commands.
