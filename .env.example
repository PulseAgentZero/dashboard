# Entivia dashboard — copy to .env.local and adjust for your environment.

# Backend API origin (no trailing slash). Baked in at docker build time.
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000
# Production (Entivia):
# NEXT_PUBLIC_API_URL=https://api.entivia.online

# cloud — Entivia Cloud (hides self-hosted-only settings tabs)
# self_hosted — on-prem / Docker (shows LLM keys, license)
NEXT_PUBLIC_DEPLOYMENT_MODE=cloud

# Marketing copy for cloud Pro plan (pricing page)
NEXT_PUBLIC_PRO_PRICE_DISPLAY=₦40,000/month

# The API must set FRONTEND_URL to this app's origin (scheme + host + port)
# for CORS and auth email links. Next.js dev defaults to port 3000:
#   FRONTEND_URL=http://localhost:3000
#
# Email verification and password reset require the API to have REDIS_URL and
# RESEND_API_KEY configured.

# Cross-subdomain URLs — leave unset for local dev (links stay relative).
# In production the docs site lives on docs.entivia.online but links to
# /auth/login and /dashboard/* need to escape the /docs nginx rewrite, so they
# must be rendered as absolute URLs pointing at the app and marketing origins.
#   NEXT_PUBLIC_APP_URL=https://app.entivia.online
#   NEXT_PUBLIC_MARKETING_URL=https://entivia.online
